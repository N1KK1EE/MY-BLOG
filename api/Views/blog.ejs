<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= blog.title %></title>
</head>
<body>
    <header>
        <h1><%= blog.title %></h1>
        <p class="author">By <%= blog.author.first_name %> <%= blog.author.last_name %> - <%= blog.author.email %></p>
    </header>

    <section class="blog-meta">
        <p><strong>Tags:</strong> <%= blog.tags.join(', ') %></p>
        <p><strong>Reading Time:</strong> <%= blog.reading_time %> minutes</p>
        <p><strong>Read Count:</strong> <%= blog.read_count %></p>
        <p><strong>Published On:</strong> <%= new Date(blog.timestamp).toLocaleDateString() %></p>
    </section>

    <section class="blog-body">
        <h2>Description</h2>
        <p><%= blog.description %></p>

        <h2>Content</h2>
        <div>
            <%= blog.body %>
        </div>
    </section>

    <footer>
        <p>Thank you for reading!</p>
        <a href="/api/blogs"></a>
    </footer>
</body>
</html>